name: CI

on:
  pull_request:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  checks:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: lint
            command: pnpm lint
          - name: typecheck
            command: pnpm typecheck
          - name: test
            command: pnpm test
          - name: build
            command: pnpm build

    name: ${{ matrix.name }}

    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm

      - name: Restore Turbo cache
        uses: actions/cache@v6
        with:
          path: .turbo
          key: ${{ runner.os }}-turbo-${{ matrix.name }}-${{ hashFiles('pnpm-lock.yaml', 'turbo.json') }}
          restore-keys: |
            ${{ runner.os }}-turbo-${{ matrix.name }}-

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Run ${{ matrix.name }}
        env:
          DATABASE_URL: ${{ secrets.TEST_DATABASE_URL }}
          SUPABASE_URL: https://vaisyqyehdkmupktgumi.supabase.co
          SUPABASE_PUBLISHABLE_KEY: sb_publishable_dVY89NBwVHYSjYPkvBgkGQ_wGLbO2uy
        run: ${{ matrix.command }}

  e2e:
    needs: checks
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared package
        run: pnpm --filter @paintrack/shared build

      - name: Set up test schema
        working-directory: apps/api
        env:
          DATABASE_URL: ${{ secrets.TEST_DATABASE_URL }}
        run: npx tsx src/db/tests/seedTestSchema.ts

      - name: Install Playwright browsers
        working-directory: apps/web
        run: pnpm exec playwright install --with-deps

      - name: Run Playwright tests
        working-directory: apps/web
        env:
          DATABASE_URL: ${{ secrets.TEST_DATABASE_URL }}
          PORT: 3000
        run: pnpm test:e2e

  migrations-drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: pnpm/action-setup@v6

      - uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check for uncommitted migration drift
        working-directory: apps/api
        env:
          DATABASE_URL: postgres://ci:ci@localhost:5432/ci
        run: |
          npx drizzle-kit generate
          git diff --exit-code supabase/migrations
